# MIA 전 기능 점검 계획 및 전수검토 (Full Feature Audit)

> Stage: 검토(檢討) · Status: `A~H 전수검토 완료` (§4) — 잔여: B4·B5 (AI 503 및 시나리오 제약으로 미검증)
> 기준일: 2026-07-17 · 최종 실측: 2026-07-24 · 결정권자: 윤겸스
> 계기: "v1, v2 모양만 그럴듯하고 모든 기능이 작동 안한다" + 폴더 선택기가 브라우저 뒤에 뜸

## 0. 점검 원칙

- **실측만 인정한다.** 단위 테스트 통과 ≠ 기능 작동. 반드시 실행 중인 서버 + 실제 UI 클릭으로 확인한다.
- **1 실패 → 1 원인 → 최소 수정 → 재검증.**
- 테스트를 약화시켜 통과시키지 않는다.
- 판정: ✅작동 / ❌고장 / ⚠️조건부(제약 있음) / ⛔미검증

## 1. 기능 인벤토리 (점검 대상 전수)

### A. 진단
| ID | 기능 | 검증 방법 |
|---|---|---|
| A1 | 진단 목록 로드 | `GET /api/list` + 카드 렌더 |
| A2 | 진단 실행 | 버튼 클릭 → 결과·건강도 갱신 |
| A3 | 카드 선택 → 상세·로그 | 클릭 → 로그 패널 |
| A4 | 상태 필터 | 필터 버튼 → 목록 변화 |
| A5 | 새 진단 생성 | `POST /api/diagnostic/create` |
| A6 | Flaky Gate(확진/의심) | 실패 진단의 `confidence` |
| A7 | 원인 후보(triage) | 실패 진단의 `causeHypotheses` |

### B. 치료
| ID | 기능 | 검증 방법 |
|---|---|---|
| B1 | 처방 생성(코드수정형) | 실패 유발 → propose |
| B2 | 처방 생성(수동처방형) | prescription 보유 진단 |
| B3 | 승인 적용 → 완치 검증 | apply → `VERIFIED_RESULT` + 디스크 반영 |
| B4 | 자동 롤백(409) | 회귀 유발 → `ROLLED_BACK` + 원상복구 |
| B5 | 진단 약화 차단 | `BLOCKED_WEAKENING` |
| B6 | 전체 치료(💉) | cure-all 요약 분류 |
| B7 | 치료 원장 | `GET /api/treatments` |

### C. 프로젝트
| ID | 기능 | 검증 방법 |
|---|---|---|
| C1 | 프로젝트 목록 | `GET /api/project/list` |
| C2 | 드롭다운 전환 | select → 목록 재로드 |
| C3 | 직접 경로 이동 | 입력 + [이동] |
| C4 | **폴더 선택기** | [선택] → **창이 최상단에 뜨는가** |
| C5 | 진단 도구 초기화 | `POST /api/project/init` |
| C6 | AI 프로젝트 분석 | explain(AI/로컬폴백) |

### D. 설정 / E. 오류패턴 / F. UI / G. 보안 / H. CLI
| ID | 기능 |
|---|---|
| D1~D3 | BYOK 조회(마스킹)·저장(키 파괴 방지)·드로어 |
| E1~E2 | 오류 패턴 목록·본문 |
| F1~F5 | V1 렌더·V2 렌더·V1↔V2 전환·도움말·토스트 |
| G1~G3 | Origin null 403·경로 탈출 차단·1MiB 제한 |
| H1~H3 | CLI run·dashboard --ui·MCP 서버 |

## 2. 확정된 결함

### ❌ C4 — 폴더 선택기가 브라우저 뒤에 뜬다 (실사용 불가)

- **증상**: [선택] 클릭 → 대화창이 브라우저/에디터 **뒤에** 생성. 사용자는 "안 열린다"고 인식.
- **근본 원인**: [backend/src/folder-picker.ps1](../../backend/src/folder-picker.ps1) 67–69행
  ```powershell
  $owner = New-Object System.Windows.Forms.Form
  $owner.ShowInTaskbar = $false
  $ownerHandle = $owner.Handle   # 핸들만 만들고 Show()/Activate() 안 함
  ```
  owner 창이 **표시·활성화되지 않아** Windows 포그라운드 정책상 소유 대화창이 앞으로 나올 수 없다.
- **방치 이력**: 같은 파일 63–66행 주석이 *"포그라운드 정책상 브라우저 뒤에 놓일 수 있다… 보조 수단으로 취급"*이라고 **인지하고도 미해결**. UI는 *"보이지 않으면 작업 표시줄을 확인하세요"* 토스트로 사용자에게 전가.
- **수정 방침**: owner를 **화면 밖(-32000,-32000) + Opacity 0 + TopMost**로 실제 `Show()`·`Activate()` → 잔상(ghost) 없이 포그라운드 자격 획득 → 대화창이 최상단에 표시. 과거 "잔상" 문제의 원인이던 *보이는 창*은 만들지 않는다.
- **수용 기준**: 대화창이 **다른 창을 가리고 최상단**에 뜬다 / 선택·취소 정상 / 잔상 없음 / `-DryRun` = `DRYRUN_OK` 유지.

## 3. C4 수정 이력 (실패 2회 → 교정)

| 시도 | 방법 | 결과 |
|---|---|---|
| 1차 | owner를 화면 밖·Opacity0·TopMost로 `Show()` + `AttachThreadInput`+`SetForegroundWindow` | ❌ 실패. 윤겸스 확인 "v1·v2 둘 다 브라우저 뒤". **오판**: Node가 띄운 자식 프로세스는 포그라운드 권한 자체가 없어 owner를 어떻게 다뤄도 안 됨 |
| 2차 | 워커 러너스페이스가 **창 제목**으로 `FindWindow` → `HWND_TOPMOST` | ⚠️ 제목 매칭 불안정(샌드박스에서 창 탐색 실패). 미검증 |
| 3차 | Add-Type P/Invoke + 워커 러너스페이스가 `GetWindow(GW_ENABLEDPOPUP)`→`HWND_TOPMOST` | ❌ **실패 + 2차 피해**. 여전히 뒤에 뜸 → 사용자는 창을 못 봄 → 5분 타임아웃이 프로세스 강제종료 → `/api/project/select` **500**. 파일의 설계 제약("compile-free, AV-safe")도 위반 |
| **4차(현행)** | **HEAD(검증된 원본) 복원 + 한 줄**: owner 핸들 생성 **전** `$owner.TopMost = $true` → `WS_EX_TOPMOST`가 창 생성에 포함 → 소유 대화창이 owner의 topmost 밴드 상속. Add-Type·러너스페이스 전부 제거(compile-free 제약 준수) | ✅ DryRun OK · 서버 동일 경로(Node execFile)에서 크래시 없이 대화창 대기 확인 · 테스트 46/46. **육안 검증 대기** |

**핵심 원리**: `WS_EX_TOPMOST`(항상 위)는 **포그라운드 권한이 필요 없다** — 백그라운드 서버의 자식 프로세스도 쓸 수 있는 유일한 z-order 수단. `SetForegroundWindow` 계열은 전부 정책상 거부됨.
**500의 정체**: 대화창이 뒤에 떠서 안 보임 → 사용자는 "안 열림"으로 인식 → 5분 타임아웃 → Node가 프로세스 kill → `err.killed` → 500. **z-order 문제와 500은 같은 뿌리.**

### ⚠️ 검증 한계 (정직 고지)
에이전트 샌드박스 셸에서는 **GUI 창을 관측할 수 없다**. 대화창이 열려 블록되는 것(exit 124)은 확인했으나, **"브라우저 앞에 뜨는지"는 윤겸스의 육안 확인이 유일한 검증 수단**이다. 자동 테스트로 대체 불가.

## 3-1. C4 최종 해결 — 아키텍처 교체 (2026-07-18, 윤겸스 A안 승인)

4차(TopMost owner)와 병렬 세션의 5번째 변형까지 **전부 실패**함으로써, OS 대화창 방식은 구현 버그가 아니라 **아키텍처 벽**으로 확정: *백그라운드 서버의 자식 프로세스는 Windows 포그라운드 정책상 대화창을 브라우저 앞에 안정적으로 띄울 수 없다.*

**해결**: OS 대화창 **완전 삭제** → **웹 내장 폴더 탐색기**로 재건축.
- 삭제: `folder-picker.ps1`(122줄), `runFolderPicker`/`parseFolderPickerOutput`, 관련 테스트 3건. `POST /api/project/select` → **410** + 안내(레거시 V1 대응).
- 신설: `GET /api/fs/list`(읽기 전용: 드라이브 루트/하위 폴더, 400 검증, 접근불가 폴더 제외) + V2 폴더 탐색 모달(브레드크럼·상위/하위 탐색·[이 폴더 선택] → 기존 `changeProject` 재사용).
- 검증 (전부 자동, 육안 의존 소멸): 백엔드 45/45 + 계약 테스트(fs/list 3종·select 410) / 린트 0 / 빌드 성공 / **브라우저 E2E 완주** — [폴더 탐색] 클릭 → 모달이 **대시보드 안에** 표시 → 상위·하위 탐색 → 선택 확정 → 모달 닫힘 → 프로젝트 변경 → 자동 진단 연쇄(진단 전 3 → 정상 3).
- **z-order 문제는 원리적으로 소멸** (창이 브라우저 밖으로 나가지 않음).

## 4. 전수검토 결과 (2026-07-24 실측 완료)

> 방법: 재건축 후 코드로 서버 기동(`--cwd examples/calculator --port 7704 --ui v2`) → HTTP 실측 + 브라우저 UI 조작.
> 원칙대로 **단위 테스트 통과를 근거로 삼지 않고** 실행 중인 서버와 실제 클릭으로 판정했다.

### 4-1. 판정표

| ID | 기능 | 판정 | 실측 근거 |
|---|---|---|---|
| A1 | 진단 목록 로드 | ✅ | `GET /api/list` 200, 3건, 필드 정상 |
| A2 | 진단 실행 | ✅ | `POST /api/run` 200, health 100%, UI 지표·필터 갱신 확인 |
| A3 | 카드 선택 → 상세 | ⚠️ | 상세(제목·id·layer·details)는 표시. OK 진단의 상태 배지가 **"미확인"**으로 뜨고 원시 로그 영역은 안내문 유지 |
| A4 | 상태 필터 | ✅ | 필터 클릭 → `aria-pressed=true`, 카드 3건 필터링 |
| A5 | 새 진단 생성 | ✅ | 200 + 목록 반영. 중복·필드누락·`../` id 전부 400 |
| A6 | Flaky Gate | ✅ | 실패 진단 `confidence=CONFIRMED` |
| A7 | 원인 후보(triage) | ✅ | `LOGIC_ASSERTION_FAILURE/HIGH` 부착 |
| B1 | 처방 생성(코드수정형) | ✅ | `strategy=local` 200, `assessment{filesTouched:1, reversible:true, touchesDiagnostics:false}`, 제안 단계 **무쓰기 보증** 확인 |
| B2 | 처방 생성(수동처방형) | ✅ | `kind=MANUAL`, prescription 배열 반환 (AI·BYOK 불필요) |
| B3 | 승인 적용 → 완치 | ✅ | `maturity=VERIFIED_RESULT`, 디스크 실제 반영, 회귀 0, 제안 재사용 404 |
| B4 | 자동 롤백(409) | ⛔ | 회귀 유발 시나리오를 로컬 룰로 만들지 못해 미검증 (단위 테스트만 존재) |
| B5 | 진단 약화 차단 | ⛔ | AI(Gemini) **503 과부하**로 AI 경로 차단, 로컬 룰 부재로 `BLOCKED_WEAKENING` 미도달 |
| B6 | 전체 치료(💉) | ✅ | cure-all 200, `cured:1`·`unprescribable:1` 정직 분류, 디스크 반영, 재진단 3/3 OK |
| B7 | 치료 원장 | ✅ | `GET /api/treatments` 기록 누적, UI "치료 검증 추세"에 실시간 반영 |
| C1 | 프로젝트 목록 | ✅ | 200, options 2건 |
| C2 | 드롭다운 전환 | ✅ | 설정 드로어 combobox 정상, 현재 프로젝트 selected |
| C3 | 직접 경로 이동 | ✅ | 유효 200 / 없는 경로 400 |
| **C4** | **폴더 선택기(웹 내장)** | ✅ | 루트·하위·parent 정상, 파일 혼입 없음, 400 검증 2종. **UI 모달이 대시보드 안에서** 상위·하위 탐색 → 선택 확정 → 프로젝트 변경 토스트까지 완주 |
| C5 | 진단 도구 초기화 | ✅ | 임시 폴더에서 200, `.vibe-clinic/diagnostics` 생성, 목록 1건 |
| C6 | AI 프로젝트 분석 | ⚠️ | 200 `success:true` 이나 `isFallback:true` (로컬 휴리스틱). AI 경로는 Gemini 503으로 미검증 |
| D1 | BYOK 조회(마스킹) | ✅ | `AUDI****fXYZ` 형태, 원본 미노출. UI 입력란은 placeholder만(계약 규칙 준수) |
| D2 | BYOK 저장(키 파괴 방지) | ✅ | 마스킹값 재저장·빈 값 저장 모두 **기존 키 보존**. 임시 프로젝트에서 검증해 사용자 실키 무손상 |
| D3 | 설정 드로어 | ✅ | 열기/닫기, 프로젝트·경로·BYOK 구획 정상 |
| E1 | 오류 패턴 목록 | ✅ | 200, 파일 목록 반환 |
| E2 | 오류 패턴 본문 | ✅ | 200 text/plain |
| F1 | V1 렌더 | ✅ | `/v1` 정상, 카드 14, V2 링크 존재 |
| F2 | V2 렌더 | ✅ | `/v2` 정상, 지표·카드·원장·오류패턴 전 구획 렌더 |
| F3 | V1↔V2 전환 | ✅ | 양쪽 상호 링크 확인 |
| F4 | 도움말 | ✅ | 모달 표시, Escape 로 닫힘 |
| F5 | 토스트 | ✅ | "진단이 완료되었습니다" / "프로젝트가 변경되었습니다" / V1 [선택] 410 안내 |
| G1 | Origin 가드 | ✅ | `null`·외부 Origin 403, localhost 200 |
| G2 | 경로 탈출 차단 | ✅ | `../../` 및 인코딩 우회 모두 404, 유출 없음 |
| G3 | 1MiB 제한 | ✅ | 413 |
| H1 | CLI run | ✅ | Health 100%, exit 0 |
| H2 | dashboard --ui | ✅ | `/`·`/v1`·`/v2` 200 |
| H3 | MCP 서버 | ✅ | initialize + tools/list 6종 정상 |

**집계: ✅ 31 · ⚠️ 3 · ⛔ 2 (총 36항목)**

### 4-2. ❌ 신규 확정 결함 — 진단이 옛 코드를 검사한다 (모듈 캐시 미무효화)

- **증상**: 대시보드 서버가 켜져 있는 동안 대상 소스를 고쳐도 재진단 결과가 바뀌지 않는다.
- **대조 실험(결정적 증거)**: `calculator.js` 의 `subtract` 를 `a + b` 로 깨뜨린 **동일 디스크 상태**에서
  - 기존 서버(7702): `health=100`, task-001 **OK** ← 거짓
  - 새로 띄운 서버(7703): `health=33`, task-001 **ERROR :: subtract(10,4) = 14** ← 정확
- **근본 원인**: [backend/src/runner.js](../../../backend/src/runner.js) 가 진단 파일(`.clinic.js`)의 캐시만 지우고, 진단이 `require` 하는 **피검사 소스의 캐시는 남겨** 첫 로드 시점 코드가 계속 쓰였다.
- **영향(심각)**: 사용자가 코드를 고쳐도 결과 불변 → "기능이 작동 안 한다"는 최초 증상과 직결. 치료의 `VERIFIED_RESULT`·회귀 감지도 옛 코드 기준이라 **가짜 완치**가 가능했다.
- **수정**: `invalidateProjectModuleCache(projectDir)` 신설 — 진단 실행 직전 프로젝트 루트 하위 모듈 캐시를 제거(`node_modules` 는 유지). Windows 대소문자 정규화 포함.
- **재검증(실서버, 양방향)**: 서버를 켜 둔 채 ①깨뜨림 → `health=33 ERROR` 감지 ②원복 → `health=100 OK` 감지. → `VERIFIED_RESULT`
- **회귀 테스트**: `runDiagnostics re-reads subject sources from disk on every run` 추가. **npm test 45 → 46 전부 통과**.
- **교훈**: 45/45 통과 상태에서도 존재하던 결함이다. 테스트는 매번 새 프로세스라 캐시가 겹치지 않아 잡히지 않았다 — §0 "테스트 통과 ≠ 기능 작동" 원칙의 실사례.

### 4-3. 관찰 사항 (결함 아님 / 후속 판단 필요)

1. ~~**치료 백업 잔여물**~~ → **수정 완료(§4-5)**. apply 후 `.bak` 이 남고 무시 대상도 아니었다.
2. ~~**A3 상태 배지 "미확인"**~~ → **수정 완료(§4-5)**. OK 진단이 "미확인"으로 표시되던 문제.
3. **로컬 룰 미보유 증상의 422**: "로컬 처방 규칙이 없습니다. AI 처방으로 다시 요청하세요"는 **정상 동작**(가짜 처방을 만들지 않음).
4. **AI 경로 전반 미검증**: 실측 시점 Gemini가 503(과부하). B5·C6의 AI 경로는 외부 요인으로 검증 불가 — 코드 결함 근거 아님.
5. **UI 절대경로 표시**: V2 헤더가 프로젝트 절대경로를 표시한다(콘솔 로그 프라이버시 규칙과는 별개 영역). U8 결정과 함께 판단.

## 5. 후속 점검(vibe-check) 결과 — 2026-07-24

§4 감사 직후 별도 점검 모드를 돌렸다. 자동 검증(린트 0 · 백엔드 47/47 · 자가진단 100%)은 전부 통과했으나,
**실패 상태의 UI**를 열어 보는 과정에서 §4가 놓친 결함 1건을 새로 확정했다.

### 5-1. ❌ 신규 확정 결함 — 실패 진단을 선택하면 V2 대시보드가 통째로 죽는다

- **증상**: 오류·주의 진단 카드를 선택하는 순간 화면이 하얗게 비고 아무 것도 조작할 수 없다. 네트워크는 전부 200이라 "백엔드는 멀쩡한데 화면만 안 나온다"로 보인다.
- **근본 원인**: [frontend/src/AppV2.jsx](../../../frontend/src/AppV2.jsx) 가 `causeHypotheses` 를 `<li>{cause}</li>` 로 렌더링했다. 이 필드는 계약상 **객체 배열**(`{cause, likelihood, signal}`)이라 React 가 예외를 던지고, error boundary 가 없어 앱 전체가 언마운트된다.
- **왜 §4에서 못 잡았나**: 정상(OK) 진단에는 `causeHypotheses` 가 없다. §4 UI 실측은 health 100% 상태에서 진행해 이 경로를 밟지 않았다. **실패를 유발한 상태로 같은 화면을 다시 열어야** 드러난다.
- **정본 참조**: 레거시 V1은 처음부터 `h.likelihood`·`h.cause`·`h.signal` 을 각각 꺼내 쓰고 있었다 — V2 재작성 때 들어간 회귀다.
- **수정**: 필드를 개별 렌더링하고 문자열 폴백을 함께 뒀다.
- **재검증(실서버)**: 소스를 깨뜨려 실패를 만든 뒤 오류 카드 선택 → 수정 전 `#root` 자식 0·body 11자 → 수정 후 **앱 생존**, 원인 후보가 `HIGH · LOGIC_ASSERTION_FAILURE — result text compares…` 로 표시, `CONFIRMED` 배지 정상. → `VERIFIED_RESULT`
- **기록**: `.vibe-clinic/error-patterns/ERR_003_react_object_child_crash.md`

### 5-2. §4 관찰 사항 처리

| 항목 | 조치 | 재검증 |
|---|---|---|
| 치료 백업 `.bak` 이 저장소를 오염 | `ensureGitignore` 를 다중 엔트리로 확장(`config.json`·`treatment-ledger.json`·`*.bak`) + 저장소 `.gitignore` 보강. 기존 항목 보존·중복 미추가 | 회귀 테스트 추가, **47/47 통과** |
| 정상 진단에 "미확인" 배지 | `confidence` 가 있을 때만 표시 | 실서버 양방향 확인 — OK 진단은 미표시, 실패 진단은 `CONFIRMED` 표시 |

`ensureGitignore` 는 그동안 `config.json` 한 줄만 보강해, **사용자 프로젝트에서는 치료 원장과 백업이 무보호**였다.
치료를 쓴 사용자의 저장소에 실행 감사 상태가 그대로 커밋될 수 있었던 갭이다.

## 6. 실패 주입 실험 + 원작(vibe-diagnosis) 이식 — 2026-07-24

§5의 교훈("실패를 만들어야 보인다")을 방법으로 굳혔다. 격리된 임시 실험실에 **10종의 실패**를
일부러 심고, 같은 날 가동 중이던 원작 `vibe-diagnosis` 대시보드(7701)를 해부해 부족한 점을 이식했다.

### 6-1. 실패 주입 매트릭스 (10종)

`prescription` 문자열 / `prescription` 배열 / 스키마 위반 / 모듈 로드 실패 / 타임아웃 /
런타임 예외 / HTML·스크립트 주입 / 5,949자 초장문 / 복합 신호 / 잘못된 status 값.

**정상 확인(결함 아님)** — 10종을 한 화면에 띄워도 앱이 살아 있었고(§5 수정 효과),
스크립트 주입은 React 이스케이프로 차단(`__XSS_FIRED=false`), 잘못된 status·타임아웃·로드 실패는
모두 사람이 읽을 수 있는 메시지로 처리됐다.

**기각된 가설(정직 기록)** — "`prescription` 이 문자열이면 UI의 `.map` 이 터진다"고 예측했으나,
백엔드가 문자열 처방을 배열로 정규화해 UI에 넘기고 있었다. 정적 분석만으로 단정하지 않길 잘했다.

### 6-2. ❌ 신규 확정 결함 2건 (뿌리가 같다 — 계약과 UI의 필드 드리프트)

| # | 증상 | 근본 원인 | 재검증 |
|---|---|---|---|
| **1** | 치료 리포트에 숫자만 나오고 상세가 `[]`. 어느 진단이 왜 그렇게 분류됐는지 알 수 없다 | UI가 `results \|\| items \|\| []` 를 찍는데 응답에는 그런 필드가 없다. 실제는 `cured/rolledBack/manual/blocked/unprescribable/held`. **폴백이 있어 예외도 안 난다** | 수정 후 "수동 처방 필요 4"(f01·f02·f04·f05), "처방 불가 6"(f03·f06~f10)이 사유와 함께 표시 |
| **2** | 설정에서 BYOK를 저장하면 이후 **모든 AI 호출이 Unknown provider 로 실패** | `SettingsDrawer` 가 `value: name \|\| id` 로 **표시명을 식별자로** 저장. 서버는 `PROVIDERS[id]` 로 조회 | 4종 전부 `id` 로 저장됨을 임시 프로젝트에서 확인(사용자 실키 무손상) |

2번은 저장이 200으로 성공하기 때문에 사용자가 원인을 알 수 없는 유형이다.
프로바이더를 4종으로 늘리면서 사용자가 설정 화면을 열 이유가 늘어 **먼저 고쳐야 할 폭탄**이었다.

기록: `.vibe-clinic/error-patterns/ERR_004_contract_ui_field_drift.md`

### 6-3. 원작 대비 해부 결과와 이식 판단

| 원작 기능 | 우리 상태 | 판단 |
|---|---|---|
| BYOK 4종(OpenAI·Anthropic·Gemini·OpenRouter) | **Gemini 전용** | **이식함** — §4 실측에서 Gemini 503 하나로 AI 경로 전체가 막혔다. 어댑터 구조가 이미 있어 비용도 낮았다 |
| 모델 입력 힌트 | 빈칸 | **이식함** — 프로바이더별 `defaultModel` 을 placeholder 로 |
| 진단 카드의 `🔗 TASK-001` | 데이터는 있는데 미표시 | **이식함** |
| QC & Prevention Scoreboard(빌드 성공률 95% 등) | 없음 | **이식 안 함** — 휴리스틱 추정치라 §0 "실측만 인정" 원칙과 충돌한다 |
| TDD Timeline / Milestone Archive | 없음 | **이식 안 함** — 수동 기록 도구로 진단·치료 범위 밖 |
| Incident History | 치료 원장이 상위 호환(회귀·롤백·maturity 기록) | 불필요 |

검증: 프로바이더 어댑터 4종이 각 API 스펙(Anthropic `system` 최상위·`max_tokens`, OpenAI 계열 `Bearer`,
Gemini `systemInstruction`)을 지키는지 **실제 호출 없이** 단위 테스트로 확인. **npm test 47 → 48**.

## 7. U4 완료 — 프론트엔드 테스트 도입 (2026-07-24, 윤겸스 승인)

§5·§6에서 새어 나간 결함이 **전부 렌더링 계층**이었고, 백엔드 테스트 48개로는 원리적으로 못 잡혔다.
사각지대를 없애기 위해 vitest + jsdom + @testing-library/react 를 도입했다.

### 7-1. 구성
- 설치: `vitest`, `jsdom`, `@testing-library/react` (frontend devDependencies, 취약점 0)
- 설정: `frontend/vite.config.js` 에 `test` 블록(`environment: 'jsdom'`, `include: test/**/*.test.jsx`)
- 스크립트: `frontend` 에 `test` 추가, 루트 `test` 를 `backend:test && frontend:test` 로 확장
- 통합 결과: **npm test = 백엔드 48 + 프론트 10 = 58건 전부 통과**

### 7-2. 무엇을 덮었나 (전부 실패 상태 렌더링)
`frontend/test/failure-states.test.jsx` — 오늘 찾은 결함 3건을 정조준한 회귀 테스트.

| 테스트 | 겨냥한 결함 |
|---|---|
| 원인 후보가 객체 배열이어도 앱이 안 죽고 필드가 보임 | §5-1 화면 크래시 |
| 원인 후보가 문자열이어도 안 죽음 | 폴백 견고성 |
| 정상 진단에 "미확인" 안 뜸 | §5-2 배지 오표시 |
| 치료 리포트가 분류별 진단 ID·사유를 보여줌 | §6-2 빈 리포트 |
| 분류가 비면 빈 상태 안내 | 빈 상태 처리 |
| 진단 카드가 linkedTask 표시 | §6-3 이식 |
| AI 서비스 선택이 표시명이 아니라 id 를 저장 | §6-2 Unknown provider |
| 선택한 서비스의 기본 모델을 힌트로 표시 | §6-3 이식 |

### 7-3. 항체가 진짜인지 검증 (빨간불 → 초록불)
새 검사가 **고치기 전 코드에서 실패하는지** 확인했다. 검증하지 않으면 아무것도 확인 안 하는 검사일 수 있다.
- `causeHypotheses` 렌더링을 결함 상태(객체째 렌더링)로 되돌림 → 관련 테스트 2건 **실패 확인**
- 원상 복원 → 전체 **통과 확인**
→ 이 테스트들은 실제 회귀를 잡는 항체다.

교훈: 백엔드 테스트는 API를 직접 부르므로 UI를 거쳐 만들어지는 값과 실패 상태 렌더링을 못 덮는다.
오늘 하루 결함 3건이 그 사각지대에서 나왔고, 이제 그 지대에 검사가 생겼다.
