MIA 모드(근본 로직 오류 잡기)로 진행합니다. 보고된 5개 증상 — ① 출력 한글화, ② BYOK 대체 용어, ③ 수리 제안 과다 지연, ④ 처방 요청 무반응(핵심), ⑤ 미초기화 프로젝트 빈 차트 — 의 근본 원인을 코드에서 추적합니다.

근본 원인 후보가 좁혀졌습니다. Diff 모달과 빈 차트 렌더 부분을 정확히 확인합니다:

**Fix C — BYOK 용어 교체:**

**Fix D — 진단 출력 한글화** (runner 플레이스홀더 + 템플릿 + 예제):

260714 저장소의 진단 소견도 한글화:

모든 수정 적용. 재빌드 + 테스트 + 라이브 검증:

라이브 검증 ① — 미초기화 임시 프로젝트에서 빈 상태 CTA와 설치 흐름:

백엔드 변경은 서버 재시작이 필요합니다. 재기동 후 E2E 재실행:

적용 실패 원인을 API로 직접 추적:

**치료 완주 성공** — 건강도 100%, 타임라인 "AI 복구 완료" 기록, 누적 치료 1건. 파일 레벨 확증 후 커밋:

윤겸스, "처방도 치료도 안 되는" 근본 원인을 **3중 버그 사슬**로 규명하고 전부 수정·검증·커밋했습니다.

---

## ✅ MIA 근본 로직 오류 수정 완료 보고

### 1. 실행한 것 / 2. 발견한 것 — 치료가 안 되던 진짜 이유 (3중 사슬)

**🔴 사슬 1 — 설정 저장이 API 키를 파괴하고 있었음 (최상위 근본)**
대시보드가 마스킹된 키(`AQ.A****…`, 정확히 12자)를 입력창에 미리 채워두고, '설정 저장'을 누르면 **그 마스킹 문자열이 진짜 키를 덮어써** 저장됩니다. 실제로 260714 프로젝트의 저장 키가 12자 마스킹 값임을 확인했고, 그래서 모든 AI 호출이 **401 인증 실패** → 처방 생성 실패였습니다.
→ 수정: 백엔드 [config-manager.js](src/config-manager.js)가 빈 값/마스킹 값 저장 시 **기존 키를 보존**, 프런트는 마스킹 키를 입력창에 채우지 않고 "🔒 저장된 키 사용 중" 안내 표시. 회귀 테스트 추가. 260714의 손상 키는 제거했습니다.

**🔴 사슬 2 — 처방이 성공해도 Diff 모달이 렌더 크래시**
백엔드는 `repairedFiles`를 **배열**로 주는데 모달은 객체 맵으로 순회 → React 렌더 붕괴 → "아무 변화 없음". → 배열 순회 + 삭제 파일 표기로 수정.

**🔴 사슬 3 — 치료가 성공해도 실패로 보고**
로드 실패 진단은 id가 파일명(`example`)인데, `.cjs` 변환 치료 후엔 모듈 자체 id(`example-diagnostic`)로 재진단에 나타나 **매칭 실패 → "치료 적용 실패" 오보**. → 수리된 파일의 실제 모듈 id로 재매칭하는 폴백 추가. 또 Node 22+에서는 ESM 로드 오류가 "Schema violation"으로 나타나 로컬 처방 엔진이 감지 못 하던 것도 구조 신호(type:module + .clinic.js 실패)로 확장.

**보조 수정**: ⑤ 빈 차트 → "🩺 진단 도구 설치(초기화)" 버튼 신설(클릭 한 번으로 해결), ③ 지연 → 시작 시 "최대 25초" 안내 토스트(+완료가 이제 눈에 보임), ① 진단 출력 한글화(runner 안내문, 템플릿, 계산기 예제, 260714 진단 소견 — 로컬 엔진 매칭용 영문은 괄호 병기로 보존), ② BYOK → **"내 AI 키 직접 연결"**(업계 표기 "자체 키 반입(BYOK)"·"내 키 직접 가져오기" 조사 후 비개발자 친화형 선택).

### 3. 수정한 것
- 본 저장소 커밋 `2c8697b` (12개 파일), 260714 커밋 `110f789`

### 4. 다시 실행한 검증
- **치료 전 과정 E2E 완주**: `type:module` 실험 프로젝트에서 진단 실패 → 처방 요청(**1초**, 로컬 엔진) → Diff 모달 정상 표시 → 승인 적용 → 디스크에 `.cjs` 변환 + `.bak` 백업 확인 → **건강도 100%, 타임라인 "AI 복구 완료", 누적 치료 1건**
- 미초기화 프로젝트 → 설치 버튼 클릭 → 예제 진단 카드 생성 확인
- `npm test` → **27/27 통과** (키 덮어쓰기 방지 회귀 테스트 포함)

### 5. 아직 실행하지 않은 것
- **260714에서 Gemini 키 재입력** — 손상된 키를 제거했으므로, 260714 프로젝트로 전환한 뒤 키를 한 번만 다시 저장해 주세요 (이제 수정 덕에 다시는 파괴되지 않습니다). 저는 정책상 키 입력을 대신할 수 없습니다.
- 대시보드는 실험용 프로젝트를 가리킨 채 `localhost:7799`에 떠 있습니다 — 좌측에서 프로젝트만 바꾸면 됩니다.

### 6. 다음 승인 필요 항목
- **push 2건**: 본 저장소 `95f9d2a`+`2c8697b`, 260714 `8a9770f`+`110f789`. 승인 시 바로 진행합니다.

Sources: [IBM — BYOK란 무엇인가요?](https://www.ibm.com/think/topics/byok), [Surfmind — BYOK 설명](https://surfmind.ai/ko/blog/byok-bring-your-own-key-future-of-ai-tools), [Microsoft Learn — 자체 키 반입(BYOK)](https://learn.microsoft.com/ko-kr/azure/key-vault/keys/hsm-protected-keys-byok)
